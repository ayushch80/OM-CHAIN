{
  "name": "blockchain",
  "version": "0.1.4",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^17.0.0",
    "vite": "^4.0.5"
  },
  "scripts": {
    "bundle": "browserify src/index.js -o src/bundle/bundle.js",
    "dev": "vite --host",
    "build": "vite build && browserify src/index.js -o src/dist/bundle/bundle.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "deta": "^1.2.0",
    "proof-of-work": "^3.3.2"
  }
}
